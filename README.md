# Proxy Applications

A GO proxy and reverse proxy application that can be self-hosted.

Please find the Docker images in the packages attached to this repository.

## Config

To change the config of the application, chang the values within the <code>newConfig</code> method under the config package. 
